% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remarks_processing.R
\name{apply_user_rules_}
\alias{apply_user_rules_}
\title{Apply user-scripted Prolog derivation rules to a data frame.}
\usage{
apply_user_rules_(df, rules_text, progress = NULL, base = 0, span = 1)
}
\arguments{
\item{df}{A data frame (typically the data after grammar/list processing).}

\item{rules_text}{User Prolog source defining \code{derive/2} (a single string).}

\item{progress, base, span}{Optional progress callback and 0..1 mapping.}
}
\value{
A data frame of derived columns (\code{nrow(df)} rows), or \code{NULL}.
}
\description{
Each row's columns are asserted as \code{cell(Name, Value)} facts; every
\code{derive(Col, Val)} solution becomes a new column (first value per Col wins).
Derived columns are typed: \code{true}/\code{false} -> logical, numeric -> numeric
(continuous), otherwise character. Returns \code{NULL} if \code{vProlog} is
unavailable or \code{rules_text} is empty.
}
