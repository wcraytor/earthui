% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_settings.R
\name{export_settings}
\alias{export_settings}
\title{Export saved earthUI settings for one file+purpose to a JSON file}
\usage{
export_settings(filename, purpose, output_json)
}
\arguments{
\item{filename}{Character scalar. The filename as stored in the DB
(e.g. \code{"Appraisal_1.csv"}, without any path prefix).}

\item{purpose}{Character scalar: \code{"general"}, \code{"appraisal"}, or
\code{"market"}.}

\item{output_json}{Character scalar. Destination file path (\code{.json}).}
}
\value{
Invisibly, the \code{output_json} path.
}
\description{
The Shiny app persists per-file, per-purpose settings in a SQLite DB
keyed by \code{"<filename>||<purpose>"}. \code{export_settings()} reads that row
and writes a single JSON file containing the full settings bundle
(target, earth parameters, variable selections, type/special overrides,
and interactions), plus an \code{rca} block for batch RCA inputs — the
subject CQA score and CQA score type.
}
\details{
If \code{output_json} already exists, the \code{rca} block of the existing file
is preserved — re-exporting from the UI does not clobber hand-edited
CQA inputs.

The emitted \code{rca} block has two fields:
\describe{
\item{cqa_score}{\code{null} or a number in \verb{[0.00, 10.00]}.}
\item{cqa_score_type}{\code{"CQA/sf"} (based on residual / living-area,
default) or \code{"CQA"} (based on residual).}
}

The emitted \code{reports} field is an array of formats to render in batch
mode: any subset of \code{"html"}, \code{"pdf"}, \code{"docx"}. An empty array \verb{[]}
(default) means no reports are generated.
}
\examples{
\dontrun{
export_settings("Appraisal_1.csv", "appraisal",
                "~/configs/Appraisal_1.json")
}
}
