% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remarks_processing.R
\name{execute_prolog_processing}
\alias{execute_prolog_processing}
\title{Execute Prolog/list processing on a data frame.}
\usage{
execute_prolog_processing(
  df,
  remarks_cols = character(0),
  list_cols = character(0),
  rules_text = NULL,
  progress = NULL
)
}
\arguments{
\item{df}{A data frame.}

\item{remarks_cols}{Character vector of column names with special "remarks".}

\item{list_cols}{Character vector of column names with special "list".}

\item{rules_text}{Optional user Prolog \code{derive/2} rules (a string) applied as
a final phase over the augmented row (see \code{\link[=apply_user_rules_]{apply_user_rules_()}}).}

\item{progress}{Optional \verb{function(fraction, detail)} progress callback,
called with \code{fraction} in 0..1 as columns are processed.}
}
\value{
\verb{list(data = <augmented df>, added = <new column names>, remarks_ok = <logical: was vProlog used>)}.
}
\description{
For every column named in \code{remarks_cols}, parse its remarks via \code{vProlog}
and add \verb{<prefix><feature>} columns. For every column in \code{list_cols}, split
its comma-delimited values into one-hot columns. Generated columns overwrite
existing same-named columns (idempotent refresh); for list columns, existing
\verb{<col>_*} columns whose value is no longer present are set all-FALSE.
}
