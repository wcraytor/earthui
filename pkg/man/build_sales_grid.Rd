% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sales_grid.R
\name{build_sales_grid}
\alias{build_sales_grid}
\title{Build a Sales Comparison Grid Excel workbook}
\usage{
build_sales_grid(
  rca_df,
  comp_rows,
  output_file,
  specials = list(),
  title_prefix = "Intermediate Sales Comparable Grid",
  progress_fn = NULL
)
}
\arguments{
\item{rca_df}{A data frame produced by the RCA workflow. Row 1 is the
subject; rows 2+ are comps. Must contain columns produced by
\code{\link[=compute_rca_adjustments]{compute_rca_adjustments()}} (\code{basis}, \code{residual}, \code{cqa}, \verb{<var>_contribution},
\verb{<var>_adjustment}, \code{net_adjustments}, \code{gross_adjustments},
\code{subject_cqa}).}

\item{comp_rows}{Integer vector of row numbers (>= 2) to include in the
grid. Maximum 30 (10 sheets, 3 comps per sheet).}

\item{output_file}{Character scalar. Destination xlsx path.}

\item{specials}{Named list mapping a special type
(e.g. \code{"contract_date"}, \code{"dom"}, \code{"latitude"}, \code{"longitude"},
\code{"area"}, \code{"concessions"}, \code{"lot_size"}, \code{"site_dimensions"},
\code{"actual_age"}, \code{"effective_age"}, \code{"living_area"}, \code{"sale_age"})
to the corresponding column name in \code{rca_df}. Default \code{list()}.}

\item{title_prefix}{Character scalar. Sheet-title prefix. Defaults to
\code{"Intermediate Sales Comparable Grid"}.}

\item{progress_fn}{Optional function called after each sheet is written
with arguments \code{sheet}, \code{total_sheets}, \code{comps_done}, \code{total_comps}.
Used by the Shiny app to drive \code{withProgress()}. Default \code{NULL}.}
}
\value{
Invisibly, the \code{output_file} path.
}
\description{
Generates a multi-sheet xlsx workbook formatted as a Sales Comparison
Grid from an RCA-adjusted data frame. Each sheet shows the subject and
up to three comparable sales side by side, with factual values, value
contributions, and adjustments per regression variable, plus rows for
grouped variables (location, site, age), residual feature inputs, and
an Adjusted Sale Price formula.
}
\details{
This is the non-Shiny computation kernel used by the earthUI Shiny app's
Sales Grid download button, and is also suitable for use from batch
scripts that already have \code{rca_df} in memory.
}
