% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_sale_age.R
\name{compute_sale_age}
\alias{compute_sale_age}
\title{Compute sale age in integer days}
\usage{
compute_sale_age(contract_vals, effective_date)
}
\arguments{
\item{contract_vals}{A vector of contract/sale dates. Accepted types:
\code{POSIXct}, \code{Date}, \code{character}, or \code{numeric} (Excel serial dates).}

\item{effective_date}{The effective (appraisal) date. Accepted types:
\code{Date}, \code{POSIXct}, or \code{character} parseable by \code{\link[=as.POSIXct]{as.POSIXct()}}.}
}
\value{
An integer vector the same length as \code{contract_vals}, giving the
number of whole days between \code{effective_date} and each contract date.
\code{NA} contract values propagate to \code{NA} results.
}
\description{
Given a vector of contract (sale) dates and a single effective date, returns
the difference in integer days (\code{effective_date - contract_date}). Contract
dates may be supplied as \code{POSIXct}, \code{Date}, character strings parseable by
\code{\link[=as.POSIXct]{as.POSIXct()}}, or numeric Excel serial date numbers (origin 1899-12-30).
}
\details{
This function is the non-Shiny computation kernel used by the
earthUI Shiny app when computing a \code{sale_age} column from a designated
\code{contract_date} column. It is also suitable for use from batch scripts.
}
\examples{
compute_sale_age(
  contract_vals = as.Date(c("2024-01-15", "2024-06-01")),
  effective_date = as.Date("2025-01-01")
)
}
