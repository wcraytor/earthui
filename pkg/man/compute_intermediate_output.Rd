% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_intermediate_output.R
\name{compute_intermediate_output}
\alias{compute_intermediate_output}
\title{Compute intermediate output data frame}
\usage{
compute_intermediate_output(
  data,
  result = NULL,
  purpose = c("general", "appraisal", "market"),
  skip_subject_row = FALSE,
  living_area_col = NULL
)
}
\arguments{
\item{data}{A data frame (the raw imported data). Must contain the target
column(s) named in \code{result$target}.}

\item{result}{A fit result list as returned by \code{\link[=fit_earth]{fit_earth()}}, or \code{NULL}.
When \code{NULL}, \code{data} is returned unchanged (the Shiny app's "non-adjusted"
export path).}

\item{purpose}{Character scalar: \code{"general"}, \code{"appraisal"}, or
\code{"market"}. Controls subject-row handling and sort behavior. Default
\code{"general"}.}

\item{skip_subject_row}{Logical. In \code{"market"} mode, indicates whether
row 1 should be treated as a subject (pinned on top during sort).
Ignored for other purposes. Default \code{FALSE}.}

\item{living_area_col}{Character scalar or \code{NULL}. If supplied and present
in \code{data}, per-SF residual and CQA_SF columns are added. Default \code{NULL}.}
}
\value{
A data frame.
}
\description{
Returns an enhanced copy of \code{data} with per-target model columns appended:
\verb{est_<target>}, \code{residual}, \code{cqa}, optional \code{residual_sf} / \code{cqa_sf} (when
a living-area column is supplied), per-g-function \verb{<var>_contribution}
columns, \code{basis}, and \code{calc_residual}. For appraisal or market purposes,
rows are sorted by \code{residual_sf} (or \code{residual}) descending, with the
subject row (row 1) pinned on top when applicable. Ranking columns
(\code{residual_sf}, \code{cqa_sf}, \code{residual}, \code{cqa}) are moved to the leftmost
positions.
}
\details{
This is the non-Shiny computation kernel used by the earthUI Shiny app's
Download Intermediate Output button, and is also suitable for use from
batch scripts.
}
