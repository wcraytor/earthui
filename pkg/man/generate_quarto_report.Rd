% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_report.R
\name{generate_quarto_report}
\alias{generate_quarto_report}
\title{Generate a Quarto report source bundle (without rendering)}
\usage{
generate_quarto_report(earth_result, dest_dir, base = "earth_report")
}
\arguments{
\item{earth_result}{An object of class \code{"earthUI_result"} as returned
by \code{\link[=fit_earth]{fit_earth()}}.}

\item{dest_dir}{Directory to write the bundle into. The bundle itself
lives at \verb{<dest_dir>/<base>_qmd/}.}

\item{base}{Bundle base name (no extension). The .qmd file inside is
named \verb{<base>.qmd}.}
}
\value{
Invisibly, the absolute path to the generated \code{.qmd} file.
}
\description{
Writes a self-contained Quarto project under \verb{<dest_dir>/<base>_qmd/}
containing the populated \verb{<base>.qmd} source, all pre-generated plots
(PNG + PDF), the report data RDS, and \code{reference.docx} for Word
rendering. The resulting bundle can be edited or combined with other
Quarto sources, then rendered to HTML / Word / PDF via
\code{\link[=convert_quarto_file]{convert_quarto_file()}}.
}
\details{
Use this when you want the Quarto source as a first-class artifact —
e.g., to combine reports from multiple projects into a master document
before publishing.
}
