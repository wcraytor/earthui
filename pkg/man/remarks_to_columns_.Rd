% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remarks_processing.R
\name{remarks_to_columns_}
\alias{remarks_to_columns_}
\title{Parse a vector of remarks into a data frame of feature columns.}
\usage{
remarks_to_columns_(
  remarks,
  prefix = "pr_",
  progress = NULL,
  base = 0,
  span = 1
)
}
\arguments{
\item{remarks}{Character vector of remark text (NAs treated as empty).}

\item{prefix}{Column-name prefix (e.g. \code{"pr_"}).}

\item{progress}{Optional \verb{function(fraction, detail)} progress callback.}

\item{base, span}{Map this call's local 0..1 progress onto \code{base + span*frac}.}
}
\value{
A data frame with \code{length(remarks)} rows, or \code{NULL}.
}
\description{
Runs the bundled DCG grammar over each remark via the \code{vProlog} engine and
returns one column per scalar feature (list-valued features are omitted).
Returns \code{NULL} if \code{vProlog} is unavailable.
}
