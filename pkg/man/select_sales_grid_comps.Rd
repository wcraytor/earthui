% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_sales_grid_comps.R
\name{select_sales_grid_comps}
\alias{select_sales_grid_comps}
\title{Select comparable sales for the Sales Comparison Grid}
\usage{
select_sales_grid_comps(
  rca_df,
  sale_age_col = "sale_age",
  min_weight = 0,
  max_gross_adj_pct = 0.25,
  max_recommended = 30L
)
}
\arguments{
\item{rca_df}{A data frame produced by the RCA workflow. Row 1 is the
subject; rows 2+ are comps. Expected columns (any may be missing; NA is
substituted): \code{id}, \code{street_address}, \code{sale_price}, \code{weight},
\code{gross_adjustments}, plus a sale-age column named by \code{sale_age_col}.}

\item{sale_age_col}{Character scalar giving the column name that holds
sale age in days. Defaults to \code{"sale_age"}.}

\item{min_weight}{Numeric. Comps with \code{weight} strictly greater than this
value are eligible. Default \code{0}.}

\item{max_gross_adj_pct}{Numeric. Comps whose \code{gross_adj_pct} is strictly
below this threshold are classed as "recommended". Default \code{0.25}
(25\%).}

\item{max_recommended}{Integer. Upper bound on the number of recommended
comps returned. Default \code{30}.}
}
\value{
A named list with:
\describe{
\item{recommended}{Data frame of recommended comps, sorted by
\code{sale_age} ascending, capped at \code{max_recommended} rows.}
\item{others}{Data frame of eligible comps not in the recommended
set, sorted by \code{gross_adj_pct} ascending.}
}
Each data frame has columns: \code{row}, \code{id}, \code{address}, \code{sale_price},
\code{sale_age}, \code{weight}, \code{gross_adj}, \code{gross_adj_pct}.
}
\description{
Given an RCA-adjusted data frame (subject in row 1, comps in rows 2+),
builds comp-summary tables, filters by weight, sorts by gross adjustment
percentage, splits into "recommended" (gross adjustment < threshold) and
"others", and caps the recommended list.
}
\details{
This is the non-Shiny computation kernel used by the earthUI Shiny app's
Sales Grid modal, and is also suitable for use from batch scripts.
}
