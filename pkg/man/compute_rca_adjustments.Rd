% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_rca_adjustments.R
\name{compute_rca_adjustments}
\alias{compute_rca_adjustments}
\title{Compute RCA (Residual Comparable Adjustment) output for an appraisal}
\usage{
compute_rca_adjustments(
  data,
  result,
  user_cqa,
  cqa_type = c("cqa", "cqa_sf"),
  living_area_col = NULL,
  weight_col = NULL
)
}
\arguments{
\item{data}{A data frame (subject + comps) matching \code{result$data} in
schema. Row 1 is the subject.}

\item{result}{A fit result list as returned by \code{\link[=fit_earth]{fit_earth()}}.}

\item{user_cqa}{Numeric scalar in \verb{[0, 9.99]} — the subject CQA score.}

\item{cqa_type}{Character: \code{"cqa"} (default) uses total-residual CQA;
\code{"cqa_sf"} uses per-SF CQA (requires \code{living_area_col}).}

\item{living_area_col}{Character scalar or \code{NULL}. If supplied and
present in \code{data}, \code{residual_sf} and \code{cqa_sf} columns are added and
\code{cqa_type = "cqa_sf"} is supported.}

\item{weight_col}{Character scalar or \code{NULL}. If supplied and present in
\code{data}, rows with \code{weight == 0} are treated as additional "subject-like"
rows that receive imputed values instead of real comp values.}
}
\value{
An enhanced data frame with model columns, contributions,
adjustments, \code{subject_value}, \code{subject_cqa}, and
\code{adjusted_sale_price}.
}
\description{
Starting from the raw data (subject in row 1, comps in rows 2+) and a
fitted earth model, produces the adjusted comparables data frame used by
the Sales Comparison Grid. The user-supplied subject CQA score is
converted into a subject residual via linear interpolation of the comp
CQA/residual curve; per-g-function contributions and adjustments are
then added for each comp, along with net/gross adjustments, percentages,
and the final \code{adjusted_sale_price}.
}
\details{
For multi-target models, the primary target drives the subject residual
calculation. Additional targets receive their own residual interpolation
and adjustment columns, imputed only for zero-weight rows.

This is the non-Shiny computation kernel used by the earthUI Shiny app's
RCA Raw Output button, and is also suitable for use from batch scripts.
}
